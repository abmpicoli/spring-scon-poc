/**
 * Events are outputs sent to some logging framework or event handling system.
 * It may be a logging system only, or some other mechanic (like, say, send events to dynatrace.
 */
package br.nom.abmpicoli.spring_scon_poc.events;